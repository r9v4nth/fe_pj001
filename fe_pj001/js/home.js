const messageForm=document.getElementById("messageForm");const composerInput=document.getElementById("composerInput");const messages=document.getElementById("messages");
if(messageForm){messageForm.addEventListener("submit",event=>{event.preventDefault();const text=composerInput.value.trim();if(!text)return;const row=document.createElement("div");row.className="bubble-row me";row.innerHTML='<div class="bubble"><div class="bubble-text"></div><div class="bubble-time">now</div></div>';row.querySelector(".bubble-text").textContent=text;messages.appendChild(row);messages.scrollTop=messages.scrollHeight;composerInput.value=""})}
